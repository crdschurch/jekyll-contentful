collections_dir: collections

collections:
  articles:
    filename: "{{ published_at | date: '%Y-%m-%d' }}-{{ slug }}"
    output: false
  testables:
    output: false
  videos:
    output: false

contentful:
  exclude:
    - migrations
